
Two ways of utilising the dynamic link library (*.dll) files provided by Thorlabs:

1) Copy *.dll files needed to this folder (e.g. Thorlabs.TLPM_64.Interop.dll)

2) Change the path value of METERPATHDEFAULT in the class.